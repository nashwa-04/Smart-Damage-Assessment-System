# خطة: AI يحلل الصور ويحدد مستوى الضرر والحالة فوراً بعد رفع التقرير

## الهدف
عندما المستخدم يرفع تقرير مع صور، **الـ AI فقط** يحلل الصور والنص ويحدد:
- `ai_damage_level` (مستوى الضرر)
- `ai_location` (الموقع الموحد)
- `ai_analysis` (الوصف التحليلي)
- `status` (الحالة → completed)

الأدمن **يشاهد** النتائج فقط ولا يمكنه تعديل مستوى الضرر.

---

## التدفق الجديد
1. المستخدم يرفع بلاغ (صور + وصف + موقع) → الحالة = `pending`
2. `AnalyzeDamageJob` يعمل فوراً عبر Queue → الحالة = `processing`
3. `GeminiService` يحلل الصورة الأولى + النص → يحدد damage_level, location, analysis
4. التقرير يتحدث → الحالة = `completed` مع نتائج AI
5. الأدمن يفتح التقرير → يشاهد مستوى الضرر محدداً من AI (للقراءة فقط)

---

## الملفات المعدلة (6 ملفات)

### 1. `app/Jobs/AnalyzeDamageJob.php` — إصلاح الصور المتعددة + معالجة الأخطاء

**المشكلة الحالية:**
- السطر 40: `$report->image_path` يكون فارغاً دائماً لأن المستخدم يرفع صور في `images[]`
- السطر 55: عند فشل AI → `status = rejected` (يجب أن يكون completed مع قيم fallback)
- السطر 20: `timeout = 60` قصير جداً لتحليل الصور

**التعديلات:**
- إضافة منطق: إذا `image_path` فارغ → استخدم أول صورة من `images`
- إذا لا توجد صورات → املأ بقيم fallback واجعل status = completed
- عند فشل AI → لا ترفض التقرير، بل املأ بقيم fallback و status = completed
- زيادة timeout إلى 120 ثانية
- في دالة `failed()`: نفس منطق fallback بدل `status = rejected`

**الكود الجديد كامل:**
```php
<?php

namespace App\Jobs;

use App\Models\Report;
use App\Services\GeminiService;
use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;
use Illuminate\Support\Facades\Log;

class AnalyzeDamageJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    protected int $reportId;
    protected int $tries = 3;
    protected int $timeout = 120;

    public function __construct(int $reportId)
    {
        $this->reportId = $reportId;
    }

    public function handle(GeminiService $geminiService)
    {
        $report = Report::find($this->reportId);

        if (!$report) {
            Log::warning('Report not found for AI processing', ['report_id' => $this->reportId]);
            return;
        }

        // Determine which image to analyze
        $imageToAnalyze = null;
        if (!empty($report->image_path)) {
            $imageToAnalyze = $report->image_path;
        } elseif (!empty($report->images) && is_array($report->images) && count($report->images) > 0) {
            $imageToAnalyze = $report->images[0];
        }

        // No image available - set fallback values
        if (!$imageToAnalyze) {
            Log::warning('No image found for AI analysis', ['report_id' => $this->reportId]);
            $report->update([
                'status' => 'completed',
                'ai_analysis' => 'لم يتم التحليل: لا توجد صور',
                'ai_damage_level' => 'low',
                'ai_location' => $report->raw_location ?? 'غير محدد',
            ]);
            return;
        }

        try {
            $report->update(['status' => 'processing']);

            $result = $geminiService->analyzeDamage(
                $imageToAnalyze,
                $report->raw_location,
                $report->raw_description
            );

            // AI determines everything: damage_level, location, analysis, status
            $report->update([
                'ai_location' => $result['normalized_location'],
                'ai_damage_level' => $result['damage_level'],
                'ai_analysis' => $result['analysis_text'],
                'status' => 'completed',
            ]);

            Log::info('AI analysis completed successfully', [
                'report_id' => $this->reportId,
                'damage_level' => $result['damage_level'],
                'location' => $result['normalized_location']
            ]);

        } catch (\Exception $e) {
            // On failure: don't reject, set fallback values and mark completed
            $report->update([
                'status' => 'completed',
                'ai_analysis' => 'فشل التحليل: ' . $e->getMessage(),
                'ai_damage_level' => 'medium',
                'ai_location' => $report->raw_location ?? 'غير محدد',
            ]);

            Log::error('AI processing failed - fallback values applied', [
                'report_id' => $this->reportId,
                'error' => $e->getMessage()
            ]);
        }
    }

    public function failed(\Throwable $exception)
    {
        $report = Report::find($this->reportId);
        if ($report) {
            $report->update([
                'status' => 'completed',
                'ai_analysis' => 'فشل التحليل: ' . $exception->getMessage(),
                'ai_damage_level' => 'medium',
                'ai_location' => $report->raw_location ?? 'غير محدد',
            ]);
        }
    }
}
```

---

### 2. `app/Http/Controllers/Admin/DashboardController.php` — إزالة ai_damage_level من الأدمن

**دالة `store` (السطر 96-152):**

| السطر | التعديل |
|-------|---------|
| 104 | **حذف**: `'ai_damage_level' => 'nullable\|in:low,medium,high,critical',` |
| 105 | **حذف**: `'status' => 'nullable\|in:pending,processing,completed,rejected',` |
| 141 | **حذف**: `'ai_damage_level' => $validated['ai_damage_level'] ?? null,` |
| 142 | **تعديل**: `'status' => 'pending',` (ثابت - AI يحدد الحالة النهائية) |

**دالة `update` (السطر 160-238):**

| السطر | التعديل |
|-------|---------|
| 168 | **حذف**: `'ai_damage_level' => 'nullable\|in:low,medium,high,critical',` |
| 230 | **حذف**: `'ai_damage_level' => $validated['ai_damage_level'] ?? $report->ai_damage_level,` |

**الرسالة (السطر 151):**
```
'تم إنشاء التقرير بنجاح. سيتم تحليله بالذكاء الاصطناعي وتحديد مستوى الضرر تلقائياً.'
```

---

### 3. `resources/views/admin/reports/create.blade.php` — إزالة حقلي الضرر والحالة

**حذف كامل (الأسطر 95-116):** قسم `ai_damage_level` select + قسم `status` select

**استبدال بـ رسالة توضيحية:**
```html
<!-- AI Auto-Assessment Notice -->
<div class="md:col-span-2">
    <div class="p-4 bg-gradient-to-r from-sand/10 to-sage/10 border border-sand/20 rounded-xl">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-sand to-sage rounded-lg flex items-center justify-center shrink-0">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                </svg>
            </div>
            <div>
                <p class="font-bold text-charcoal">تقييم تلقائي بالذكاء الاصطناعي</p>
                <p class="text-charcoal/60 text-sm">سيتم تحليل الصور وتحديد مستوى الضرر والحالة تلقائياً بعد إنشاء التقرير</p>
            </div>
        </div>
    </div>
</div>
```

---

### 4. `resources/views/admin/reports/edit.blade.php` — تحويل الضرر لقراءة فقط

**حذف (الأسطر 297-309):** قسم select الخاص بـ `ai_damage_level`

**استبدال بـ عرض للقراءة فقط:**
```html
<!-- Damage Level - Read Only (AI Only) -->
<div>
    <label class="block text-sm font-bold text-slate-700 mb-2">
        <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-purple-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
            مستوى الضرر (تقييم AI)
            <span class="text-xs font-normal text-slate-400 bg-purple-50 px-2 py-0.5 rounded-full">للقراءة فقط</span>
        </span>
    </label>
    @php
    $currentLevel = $report->ai_damage_level;
    $damageInfo = [
        'low' => ['label' => 'منخفض', 'color' => 'bg-green-50 border-green-200 text-green-700', 'icon' => '🟢'],
        'medium' => ['label' => 'متوسط', 'color' => 'bg-yellow-50 border-yellow-200 text-yellow-700', 'icon' => '🟡'],
        'high' => ['label' => 'عالي', 'color' => 'bg-orange-50 border-orange-200 text-orange-700', 'icon' => '🟠'],
        'critical' => ['label' => 'حرج', 'color' => 'bg-red-50 border-red-200 text-red-700', 'icon' => '🔴'],
    ];
    @endphp
    <div class="p-4 rounded-xl border-2 {{ $currentLevel && isset($damageInfo[$currentLevel]) ? $damageInfo[$currentLevel]['color'] : 'bg-gray-50 border-gray-200 text-gray-500' }}">
        @if($currentLevel && isset($damageInfo[$currentLevel]))
            <p class="text-lg font-bold">{{ $damageInfo[$currentLevel]['icon'] }} {{ $damageInfo[$currentLevel]['label'] }}</p>
            <p class="text-sm mt-1">تم التحديد بواسطة الذكاء الاصطناعي</p>
        @else
            <p class="text-slate-500">لم يتم التحليل بعد</p>
        @endif
    </div>
</div>
```

**أيضاً في نفس الملف — إزالة حقل status select (الأسطر 312-321):**
استبدال بعرض للقراءة فقط مشابه يبين حالة التقرير بدون إمكانية التعديل.

---

### 5. `resources/views/user/reports/create.blade.php` — توضيح رسالة AI

**السطر 149-150:** تحسين الرسالة الحالية:
```
قبل: <p class="text-[#0B0B45]/60 mt-2">أدخل تفاصيل البلاغ وسيتم تحليله بالذكاء الاصطناعي</p>
بعد: <p class="text-[#0B0B45]/60 mt-2">أدخل تفاصيل البلاغ - سيتم تحليل الصور وتحديد مستوى الضرر تلقائياً بالذكاء الاصطناعي</p>
```

---

### 6. `app/Services/GeminiService.php` — تحسين الـ prompt لتحليل أفضل

**السطر 22:** تحسين الـ prompt ليعطي نتائج أدق بالعربي:
```
قبل: "Analyze the image and text. 1. Normalize location in Syria. 2. Assess damage level 1-10. 3. Extract description. Return JSON with: normalized_location, damage_level (low/medium/high/critical), analysis_text."

بعد: "You are a damage assessment AI for Syria. Analyze the provided image and text data:
1. Normalize the location name to a standard Syrian city/neighborhood name.
2. Assess the damage level based on the image: low (minor cracks), medium (moderate structural damage), high (major structural failure), critical (total collapse).
3. Write a detailed analysis in Arabic describing the visible damage.

Return ONLY valid JSON: {\"normalized_location\": \"...\", \"damage_level\": \"low|medium|high|critical\", \"analysis_text\": \"...\"}"
```

**السطر 35:** إضافة كشف تلقائي لنوع الصورة:
```php
قبل: 'mime_type' => 'image/jpeg',
بعد: 'mime_type' => mime_content_type(storage_path('app/public/' . $imagePath)) ?: 'image/jpeg',
```

---

## ملف لا يحتاج تعديل
- ✅ `User/DashboardController.php` — تم تعديله سابقاً (`AnalyzeDamageJob::dispatch` موجود في السطر 99)

---

## بعد التنفيذ — خطوة ضرورية
شغّل queue worker في terminal منفصل:
```bash
cd "D:\Smart Damage Assessment System\backend"
php artisan queue:work
```

بدون هذا الأمر، الـ Jobs لن تعمل والـ AI لن يحلل شيئاً.
