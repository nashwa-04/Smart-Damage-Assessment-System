# دليل البدء السريع - اختبارات تكامل API

## نظرة عامة

هذا الدليل يساعدك على تشغيل اختبارات تكامل API لنظام "تقييم الأضرار الذكي" باستخدام سكربتات cURL الأوتوماتيكية.

## الملفات المرفقة

| الملف                                                              | الوصف                     | المنصة    |
| ------------------------------------------------------------------ | ------------------------- | --------- |
| [`test_api_integration.sh`](test_api_integration.sh)               | سكربت Bash للينكس/ماك/WSL | Unix-like |
| [`test_api_integration.bat`](test_api_integration.bat)             | سكربت Batch لويندوز       | Windows   |
| [`API_INTEGRATION_TEST_REPORT.md`](API_INTEGRATION_TEST_REPORT.md) | تقرير الاختبارات المفصل   | توثيق     |
| [`TEST_SUITE_README.md`](TEST_SUITE_README.md)                     | دليل شامل (بالإنجليزية)   | توثيق     |
| [`دليل_البدء_السريع.md`](دليل_البدء_السريع.md)                     | هذا الملف                 | توثيق     |

## البدء السريع

### لمستخدمي Windows

```cmd
# تشغيل السكربت
test_api_integration.bat
```

### لمستخدمي Linux/macOS/WSL

```bash
# جعل السكربت قابلاً للتنفيذ
chmod +x test_api_integration.sh

# تشغيل الاختبار
./test_api_integration.sh
```

## التكوين

### عنوان السيرفر

السكربتات مُهيأة لاستخدام عنوان IP الخاص بك:

```
http://10.28.57.151:8000/api
```

### بيانات الدخول

```
البريد الإلكتروني: user@test.com
كلمة المرور: password
```

## ما الذي يختبره السكربت؟

| #   | الاختبار            | النقطة النهائية        | الوصف                       |
| --- | ------------------- | ---------------------- | --------------------------- |
| 1   | الإعداد             | N/A                    | إنشاء صورة اختبار           |
| 2   | فحص الصحة           | `GET /`                | التأكد من أن الـ API يعمل   |
| 3   | تسجيل الدخول        | `POST /login`          | المصادقة والحصول على التوكن |
| 4   | جلب بيانات المستخدم | `GET /user`            | التحقق من صلاحية التوكن     |
| 5   | إنشاء تقرير         | `POST /reports`        | رفع تقرير أضرار ⭐          |
| 6   | عرض التقارير        | `GET /reports`         | جلب جميع التقارير           |
| 7   | عرض تقرير محدد      | `GET /reports/{id}`    | جلب تفاصيل تقرير            |
| 8   | حذف تقرير           | `DELETE /reports/{id}` | حذف تقرير الاختبار          |
| 9   | تسجيل الخروج        | `POST /logout`         | إنهاء الجلسة                |

## بيانات الاختبار

### البيانات العربية المستخدمة

**الموقع:**

```
دمشق - منطقة المزة - شارع الفيلات الغربية - بناء رقم 14
```

**الوصف:**

```
يوجد تصدع كبير في الجدار الخارجي الشمالي للمبنى يمتد من الطابق الأول حتى الثالث، مع تساقط أجزاء من الشرفة. يرجى التقييم العاجل لاحتمالية السقوط.
```

**الإحداثيات:**

- خط العرض: 33.5138 (دمشق، سوريا)
- خط الطول: 36.2765

## فهم المخرجات

### مثال على نجاح الاختبار

```
========================================
5. Create Report
========================================

[INFO] Sending report with Arabic data:
  Location: دمشق - منطقة المزة - شارع الفيلات الغربية - بناء رقم 14
  Coordinates: 33.5138, 36.2765
  Description: يوجد تصدع كبير في الجدار الخارجي الشمالي للمبنى يمتد من الطابق الأول حتى الثالث، مع تساقط أجزاء من الشرفة. يرجى التقييم العاجل لاحتمالية السقوط.

[OK] Report created successfully (HTTP 201)
[INFO] Report ID: 1
{"id":1,"user_id":1,"latitude":33.5138,"longitude":36.2765,...}
```

### مثال على فشل الاختبار

```
========================================
3. Login
========================================

[FAIL] Login failed or no token found (HTTP 401)
{"message":"Invalid credentials"}
```

## حل المشاكل الشائعة

### 1. رفض الاتصال

**الخطأ:** `curl: (7) Failed to connect to 10.28.57.151 port 8000`

**الحل:**

- تأكد من تشغيل سيرفر Laravel:
  ```bash
  cd backend
  php artisan serve --host=10.28.57.151 --port=8000
  ```
- تحقق من إعدادات الجدار الناري
- تأكد من صحة عنوان IP

### 2. فشل المصادقة

**الخطأ:** `{"message":"Invalid credentials"}`

**الحل:**

- تأكد من وجود المستخدم في قاعدة البيانات:
  ```bash
  php artisan tinker
  >>> User::all()
  ```
- شغّل Database Seeder:
  ```bash
  php artisan db:seed --class=UserSeeder
  ```
- تحقق من صحة البريد الإلكتروني وكلمة المرور

### 3. فشل رفع الصورة

**الخطأ:** `The image field is required`

**الحل:**

- تأكد من إنشاء صورة الاختبار بنجاح
- تحقق من صلاحيات الملفات
- تأكد من إرسال البيانات بصيغة multipart/form-data

### 4. تشويش النص العربي

**الخطأ:** ظهور الأحرف العربية كـ `????` أو مشوشة

**الحل:**

- تأكد من أن ترميز قاعدة البيانات هو UTF-8
- تحقق من إعدادات اللغة في `config/app.php`
- تأكد من أن الترميز في التيرمينال يدعم UTF-8

### 5. فشل استخراج التوكن

**الخطأ:** `[FAIL] Login succeeded but no token found in response`

**الحل:**

- قم بتثبيت `jq` لتحليل JSON بشكل أفضل
- تحقق من تنسيق استجابة الـ API
- تأكد من إعداد Sanctum بشكل صحيح

## تشغيل أوامر cURL يدوياً

للتصحيح، يمكنك تشغيل الأوامر يدوياً:

### فحص الصحة

```bash
curl -X GET "http://10.28.57.151:8000/api/" \
  -H "Accept: application/json"
```

### تسجيل الدخول

```bash
curl -X POST "http://10.28.57.151:8000/api/login" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{"email":"user@test.com","password":"password"}'
```

### إنشاء تقرير (باستخدام التوكن)

```bash
curl -X POST "http://10.28.57.151:8000/api/reports" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Accept: application/json" \
  -F "image=@damage_test.jpg" \
  -F "latitude=33.5138" \
  -F "longitude=36.2765" \
  -F "raw_location=دمشق - منطقة المزة" \
  -F "raw_description=تقرير اختبار"
```

## تخصيص الاختبارات

### تغيير بيانات الاختبار

عدّل البيانات في السكربت:

```bash
# في دالة test_create_report
LATITUDE="34.5678"
LONGITUDE="35.6789"
RAW_LOCATION="موقعك المخصص"
RAW_DESCRIPTION="وصفك المخصص"
```

## التحقق من النتائج

بعد انتهاء الاختبار، ستحصل على ملخص:

```
========================================
Test Execution Summary
========================================
Total Tests: 9
Passed: 9
Failed: 0

Test Results:
  Setup:✓
  Health Check:✓
  Login:✓
  Get User:✓
  Create Report:✓
  List Reports:✓
  Show Report:✓
  Delete Report:✓
  Logout:✓

All tests passed successfully!
```

## نصائح إضافية

### قبل تشغيل الاختبار

1. تأكد من تشغيل سيرفر Laravel
2. تأكد من وجود قاعدة البيانات
3. شغّل الـ Seeder لإنشاء المستخدم:
   ```bash
   php artisan db:seed --class=UserSeeder
   ```

### بعد تشغيل الاختبار

1. راجع ملف السجلات: `backend/storage/logs/laravel.log`
2. تحقق من قاعدة البيانات: `php artisan tinker`
3. تأكد من حذف بيانات الاختبار (يقوم السكربت بذلك تلقائياً)

## الموارد الإضافية

- [توثيق API](API_DOCUMENTATION.md)
- [دليل إعداد Backend](backend/SETUP.md)
- [إرشادات الوكلاء](AGENTS.md)
- [تقرير الاختبار الشامل](API_INTEGRATION_TEST_REPORT.md)

## الدعم

إذا واجهت مشاكل:

1. تحقق من قسم [حل المشاكل الشائعة](#حل-المشاكل-الشائعة)
2. راجع [توثيق API](API_DOCUMENTATION.md)
3. تحقق من سجلات Laravel: `backend/storage/logs/laravel.log`
4. تحقق من حالة قاعدة البيانات: `php artisan tinker`

---

**آخر تحديث:** 2026-01-27  
**الإصدار:** 1.0.0  
**بيئة الاختبار:** التطوير (سيرفر محلي)  
**عنوان السيرفر:** 10.28.57.151:8000
